import { TransitionPresentation } from '../types';

export type CustomPresentationProps = {
    width: number;
    height: number;
};
export declare const star: (props: CustomPresentationProps) => TransitionPresentation<CustomPresentationProps>;
