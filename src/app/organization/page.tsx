import { metaObject } from "@/config/site.config";

export const metadata = {
  ...metaObject("Organization Dashboard"),
};

export default function OrganizationPage() {
  return <div>Organization</div>;
}
